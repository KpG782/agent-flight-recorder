{
  "name": "agent-flight-recorder",
  "private": true,
  "version": "0.1.0",
  "description": "Agent Flight Recorder — forensic black-box recorder for agent/app runs (VideoDB hackathon)",
  "packageManager": "pnpm@10.33.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=20"
  }
}
