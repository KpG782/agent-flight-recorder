# Agent Flight Recorder — apps/api runtime deps (fixed stack, PLAN.md).
# Mirror of pyproject.toml [project.dependencies] for pip-only setups.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
structlog>=24.4.0
redis>=5.2.0
videodb[capture,websockets]>=0.2.0
anthropic>=0.40.0
supabase>=2.10.0
asyncpg>=0.30.0
python-dotenv>=1.0.1
